# ADMG-gen
ADMG-Gen: A random ADMG generator and identification oracle using ananke Generates random Acyclic Directed Mixed Graphs (ADMGs) and runs ananke's OneLineID algorithm to determine if p(Y|do(X)) is identified and compute the exact identifying functional. Built as a ground truth dataset generator for causal inference benchmarking.
